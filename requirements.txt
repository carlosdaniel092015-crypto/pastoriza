# Núcleo
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0

# Agente
# openai-agents fijado: RedisSession implementa a mano su protocolo Session; un
# cambio de firma en un rebuild rompería producción sin tocar código propio.
openai-agents==0.19.4
openai>=1.60.0

# Infra
redis>=5.2.0
httpx>=0.27.0
structlog>=24.4.0
# Windows no trae base de datos IANA de zonas horarias: zoneinfo (app/horario.py)
# la necesita para "America/Santo_Domingo", si no ZoneInfo(...) explota al importar.
# En Linux/Docker no estorba (ya está la del sistema, apt tzdata en el Dockerfile).
tzdata>=2024.1
# Recepción de archivos en el panel (respuesta con imagen/cámara). FastAPI lo
# necesita para UploadFile/Form.
python-multipart>=0.0.9
# Conversión de imágenes de Odoo (webp) a JPG para WhatsApp, sin proxy externo.
Pillow>=10.0.0

# Dev / tests
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
